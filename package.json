{
  "name": "road-hazard-app",
  "version": "1.0.0",
  "description": "Real-time road hazard reporting system",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start"
  },
  "keywords": ["road", "hazard", "real-time", "maps"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
